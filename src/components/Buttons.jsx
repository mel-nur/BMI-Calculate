import styled from "styled-components"

export const HesaplaButton = styled.button`
   background: teal;
   color: white;
   padding: 10px 20px;
   border: none;
   border-radius: 8px;
   cursor: pointer;
   &:hover {
      background: #005f5f;
   }
`
